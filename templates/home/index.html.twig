{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    <h1>Home</h1>

    <p>Bienvenue</p>

    {% for flash in app.flashes %}
        {% for message in flash %}
            <p>{{ message }}</p>
        {% endfor %}
    {% endfor %}

{% endblock %}
